# Hacker News Action Aliases

These aliases normalize model-shaped action names into the tool's supported action contract.
They are not routing keywords; they only repair the `action` parameter after the Hacker News tool has already been selected.

show_item = comments
item = comments
story = comments
story_detail = comments
details = comments
detail = comments
deep_dive = comments
deepdive = comments
fetch = top
browse = top
stories = top
