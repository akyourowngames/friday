# File Path Aliases

These aliases normalize model-shaped path arguments after a file tool has
already been selected. They are not routing rules.

## Aliases

current folder = .
current directory = .
this folder = .
this directory = .
here = .
