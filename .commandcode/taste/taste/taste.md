# Taste
- Also uses Cline as an AI coding tool and keeps its session transcripts; asks the assistant to locate and read the latest Cline session and continue its work (expects cross-tool continuity). Cline sessions live in `C:\Users\anime\AppData\Roaming\Code\User\globalStorage\saoudrizwan.claude-dev\tasks` and may belong to unrelated projects, so check task metadata before assuming relevance. Confidence: 0.6
- Develops on Windows: PowerShell is the shell, home dir is `C:\Users\anime`, and active projects live under Desktop (e.g. TypeScript repo `friday-ng`). Confidence: 0.8
- Wants to ship `friday-ng` as a public npm package so end users can `npm install -g friday-ng` and run it from anywhere by typing `friday-ng`; expects the `bin` field to expose the CLI entrypoint. Confirmed package name `friday-ng` (unscoped), MIT license, version 0.3.0. Confidence: 0.9
- Prefers the assistant to handle publish-readiness prep itself (bump version, write LICENSE, add `.npmrc`, dry-run pack) but NEVER to run `npm login` or touch credentials — the user drives auth in a separate shell and signals when done. Confidence: 0.85
- Sends terse, casual, lowercase requests (typos common, e.g. "contieu", "hanress", "di for and for repsonses", "its jsut raw md its not rendered"); expects the assistant to infer missing specifics (like which app a file belongs to, or where session files live) instead of asking for clarification. Resumes in-progress work with single-word prompts like "continue". When a request is ambiguous, the user often corrects the interpretation in the next message rather than phrasing the original more carefully. Confidence: 0.75
- Wants **properly formatted responses with headings, sub-headings, bullet points, and code blocks** — explicitly does NOT want raw text dumps of file lists or JSON, especially for web/app work. Also cares about this in the **friday product itself**: the TUI must render Markdown structure (headings, code fences, bullets, tables) in the final assistant reply box, not display raw markdown source. Streaming should show raw text with cursor during deltas; full render happens once on `message_end`. The bar is **real visual structure** — colored accent bars on headings, colored `•` / `1.` markers on lists, dim `│` on quotes, indented nested bullets, dim `─` rules — not just bold text or a `•` prefix. The user will push back ("still raw output") if a fix only makes the parser recognize Markdown without making the rendered output actually look like Markdown. Confidence: 0.95
- GitHub username is `akyourowngames` — expects the assistant to push local changes to that GitHub account when asked ("push all local changes to github as akyourowngames"). Confidence: 0.8
- Works on `friday-ng` as a **multi-surface product** (TUI + Next.js web chat in `web/`). When a request is ambiguous between surfaces, the user corrects tersily ("i am talkinng about web version bro") rather than specifying up front; expects the assistant to pivot quickly to the correct surface without re-asking. Defaults to thinking in terms of the TUI when not specified. Confidence: 0.7
- Calls the product "our AI or harness" interchangeably — the agent harness and the model are the same thing in their mental model. Confidence: 0.7
- For any external capability (web search, etc.), wants it to be **strong on par with commercial products**: multiple providers with fallback/parallel race, refined structured output (summary + numbered references with title/snippet/url, not a raw URL list), and no-key options alongside paid-key options. A single weak provider that "sometimes works" is unacceptable — they will ask to "strong this one" and want DuckDuckGo/Tavily/SearXNG-style breadth. Applies broadly: any time a tool or integration feels thin, they expect a multi-source, refined-result upgrade, not a tweak. Confidence: 0.85
- Uses project-local `todo.txt` files as a task source of truth and issues short imperatives like "read todo.txt and implement things" — wants the assistant to read the file, build the todos in the todo system, and execute end-to-end without further planning chitchat. When work is already in flight, treats fresh "continue" prompts as a resume signal, not a chance to re-plan. Confidence: 0.8
- When adding a new power-user feature to the web chat, expects a complete end-to-end shape: server route, client component, transcript card in the message stream, topbar/palette entry, smoke test, and a build/typecheck pass — not just the route handler. Likes the pattern of a `web/scripts/smoke.mjs` loopback check + `npm run smoke` script as the verification layer for thin route handlers. Confidence: 0.8
- Prefers the existing in-process `runShell` executor + `permissions.ts` dangerous-command guard for any new command-execution surface (Quick Run, etc.) — reuse over re-implementation, even if it means accepting a `cmd /c` quoting limitation. Confidence: 0.75
- Writes commit messages as a `commit-msg.txt` file and pipes them through `git commit -F commit-msg.txt`, then pushes to `codex/friday-ng` on `akyourowngames`'s GitHub. Often needs a follow-up `git rm commit-msg.txt` cleanup commit; that's a known friction point in their workflow, not a request to change it. Confidence: 0.85
